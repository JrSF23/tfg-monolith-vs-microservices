\# KPI ↔ Outils de mesure

\- Latence p95 (ms) : JMeter

\- Throughput (req/s) : JMeter

\- CPU (%) : Prometheus/Grafana (ou docker stats)

\- RAM (MB/GB) : Prometheus/Grafana (ou docker stats)

\- Taux d’erreurs (% 4xx/5xx) : JMeter

